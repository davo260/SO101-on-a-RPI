#!/bin/sh
# Wait up to 30 s for both arms (udev symlinks) before starting so101d.
for i in $(seq 30); do
    [ -e /dev/so101_leader ] && [ -e /dev/so101_follower ] && exit 0
    sleep 1
done
echo "so101: /dev/so101_leader or /dev/so101_follower missing" >&2
exit 1
